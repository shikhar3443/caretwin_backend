version: '3.8'

services:
  backend:
    build: .
    container_name: caretwin_backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./caretwin.db
    volumes:
      - ./uploads:/app/uploads
